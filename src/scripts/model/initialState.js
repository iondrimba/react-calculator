import MapKeys from './mapKeys';

const defaultStore = {
    historyDisplay: '',
    displayValue: 0,
    keyDown: '',
    muted: false,
    calculated: false,
    keys: MapKeys
};

export default defaultStore;
